name: Clear Workspace
description: Clear workspace on github actions self-hosted runner
runs:
  using: "composite"
  steps:
    - shell: bash 
      run: |
        echo "Cleaning workspace directory..."
        sudo rm -rfv ${{ github.workspace }}
        mkdir -p ${{ github.workspace }}
      
